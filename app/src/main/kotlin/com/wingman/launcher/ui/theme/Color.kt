package com.wingman.launcher.ui.theme

import androidx.compose.ui.graphics.Color

// ── Wingman Palette ──────────────────────────────────────────────────────────
val ColorBackground     = Color(0xFF0A0F0A)   // near-black dark green
val ColorPrimary        = Color(0xFFD4821A)   // amber/orange — active highlight
val ColorSecondary      = Color(0xFF4A7C4A)   // muted green text
val ColorDimText        = Color(0xFF2A4A2A)   // very dim green — inactive items
val ColorHighlightBar   = Color(0xFFB86B2B)   // selection bar fill
val ColorBlack          = Color(0xFF000000)
val ColorWhite          = Color(0xFFFFFFFF)
val ColorGlowOrange     = Color(0xFFD4821A)   // glow at 60 % alpha

// ── High-Contrast variant extras ─────────────────────────────────────────────
val ColorHCPrimary      = Color(0xFFFFAA00)
val ColorHCSecondary    = Color(0xFF66BB66)
val ColorHCBackground   = Color(0xFF000000)
